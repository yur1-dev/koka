// // Similar to wallet/route.ts, but:
// const user = await prisma.user.update({
//   where: { id: body.userId },
//   data: { walletAddress: publicKey },
// });
// // Then generate new JWT with walletAddress
